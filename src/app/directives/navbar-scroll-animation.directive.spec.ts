import { NavbarScrollAnimationDirective } from './navbar-scroll-animation.directive';

describe('NavbarScrollAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarScrollAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
